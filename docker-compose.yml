services:
  ctyun-alive:
    image: ghcr.io/swcoffee/ctyun_alive:latest
    container_name: ctyun-alive
    restart: always
    environment:
      - ACCOUNT=15326906619
      - PASSWORD=Aa5733553.
      - DOCKERMODE=true
      - INTERVAL_MINUTE=45
      - ALIVE_SECOND=100
    volumes:
      - ./data:/app/data
